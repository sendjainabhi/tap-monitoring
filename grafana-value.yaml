datasources:
  datasources.yaml:
    apiVersion: 1
    datasources.yaml:
      - name: prometheus
        type: prometheus
        url: http://prometheus-server.prometheus.svc.cluster.local
        access: proxy
        isDefault: true